resource:
  - slice:
      - fabric_slice:
          - provider: '{{ fabric.fabric_provider }}'
            name: '{{ var.slice_name }}'
  - slice:
      - chi_slice:
          - provider: '{{ chi.chi_provider }}'
            name: '{{ var.slice_name }}'
